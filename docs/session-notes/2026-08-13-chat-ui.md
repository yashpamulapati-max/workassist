# Work Assist — Session Notes (2026-08-13)

Chat UI work on the live `ams-ok-yp2` embed. Permanent pod/image fix is **paused until next week**.

Figma: [Work Assist – Chat Panel (Docked / Expanded / Collapsed)](https://www.figma.com/design/Nptec5oos7wpdHuNgIhvJ1/Work-Assist-%E2%80%93-Chat-Panel--Docked---Expanded---Collapsed-?node-id=0-1)

Source in the AgileAssets repo: `ams-web/src/main/webapp/Kernel/trimble-assist.js` + `trimble-assist.css` (ephemeral `kubectl cp` to the pod).

---

## Shipped on yp2

### Expand / restore (Figma docked ↔ expanded)

- **Docked:** 420×600 (existing floating panel).
- **Expanded:** 760×860, ~220ms CSS transition.
- Header **expand** (four-corner icon) / **restore** (arrows-in).
- FAB still opens; **X** still closes (resets to docked).
- Iframe stays mounted — drafts persist.

### Where-you-are chip

- Outer blue bar no longer duplicates the inner “Work Assist” title.
- Shows current window (`document.title` / `basic_window.GetTitle()`, plus active nav when available).
- Refreshes every 2.5s while the panel is open.

### Page-aware prompt chips — **removed 2026-08-14**

Shipped 2026-08-13, then removed at user request (chips were obvious / noisy). Location chip remains.

### Slim chrome (kill the double header)

- Our header is **36px**: location + expand / restore / close only.
- Trimble’s inner Work Assist header stays inside the iframe.

---

## Explicitly not in this pass

- Figma **Collapsed** (title-bar only) — still prototype-only.
- Permanent `agents` scope + embed files in the image — parked next week (Docker available locally).
- Replacing Trimble’s in-iframe starter cards (those live in Agent Studio, not the parent).

---

## GitHub Project

Board: [workassist (Project #1)](https://github.com/users/yashpamulapati-max/projects/1)

Keep this board current whenever we ship or park work.
